apiVersion: apps/v1
kind: Deployment
metadata:
  name: be
spec:
  replicas: 3
  selector: 
     matchLabels:
       env: prod
  # strategy:
  #   type: Recreate
    # rollingUpdate:
    #   maxSurge: 1
    #   maxUnavailable: 1
  template:
    metadata:
      labels:
        env: prod
    spec:
      containers:
       - name: nginx
         image: menamagdyhalem/simple-webserver
         ports:
           - containerPort: 3000
         #image: img-test
         #imagePullPolicy: Never

